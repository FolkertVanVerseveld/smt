#!/bin/bash -e
CC=gcc
if hash colorgcc 2>/dev/null; then
	CC=colorgcc
fi
cstd='-std=gnu99'
CFLAGS="-Wall -Wextra -pedantic $cstd -fPIC -O2 -ffast-math -g $*"
libs='sdl2 gl'
if ! hash pkg-config 2>/dev/null; then
	echo "pkg-config is missing" 1>&2
	exit 1
fi
CFLAGS="$CFLAGS $(pkg-config --cflags $libs)"
LDLIBS="$(pkg-config --libs $libs)"
LLIBS='-lSDL2_image -lSDL2_mixer -lm'
CCM="$CC $cstd -MM"
cat <<'EOF'>.gitignore
.gitignore
*.tmp
# Make
Makefile
# CC
*.o
*.a
*.so
# VIM
*.vim
*.swp
# valgrind
*.valgrind
*.log
# archives
*.tgz
*.zip
EOF
cat <<EOF >Makefile
.PHONY: default clean

CC=$CC
CFLAGS=$CFLAGS
LDLIBS=$LDLIBS $LLIBS

EOF

printf "OBJECTS=" >>Makefile
for i in *.c; do
	printf ' \\\n\t%s' "${i/%.c/.o}" >>Makefile
done

cat <<'EOF' >>Makefile


default: libgut.a libgut.so $(DEMOS)
libgut.a: $(OBJECTS)
	ar -cr libgut.a $(OBJECTS)
libgut.so: $(OBJECTS)
	$(CC) -shared -Wl,-soname,libgut.so -o libgut.so $(OBJECTS)
%.o: %.c
	$(CC) -c $< -o $@ $(CFLAGS)
clean:
	rm -f libgut.a libgut.so
	rm -f $(OBJECTS)
EOF
